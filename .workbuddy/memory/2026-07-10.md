# 2026-07-10

## TaskManager 临时事项排序与计划调整补录

修改文件：`D:/workspace/TaskManager/index.html`

完成内容：
1. 为新建临时事项和新建延期记录增加 `createdAt` 时间戳，编辑时保留原时间戳。
2. 临时事项列表按日期倒序、同日期按 `createdAt` 倒序排列（后输入的在前）。
3. 延期记录在重算和审阅时间线中按日期正序、同日期按 `createdAt` 正序排列，确保同日调整点与延期记录的先后关系正确。
4. 计划调整表单增加「调整日期」字段，可补录前几天做的调整；保存/编辑时写入 `record.date`，不再强制使用今天。

后续注意：
- 旧数据没有 `createdAt` 的，排序时回退到空字符串，同日期下保持原有相对顺序。
- 若需让调整点位于同日某条延期之前，应先录入调整记录，再录入当日延期。

## 「审阅」标签页改为「汇总」

修改文件：`D:/workspace/TaskManager/index.html`（同文件）

完成内容：
1. 标签页「审阅」改名「汇总」，卡片标题改为「任务汇总」。
2. 新增「汇总方向」切换：按项目号 / 按日期。
3. 方向1 按项目号：复用项目下拉筛选，汇总所选项目的延期情况。
4. 方向2 按日期：快捷按钮 上周/本周/上月/本月/今年 + 自定义起止日期；按延期记录 `date` 落在范围内过滤。
5. 顶部总数据：任务总数、有延期任务数、延期记录数、计划调整次数、临时事项延期数、总延期时长（天/小时）。
6. 列表每行展示 延期记录/调整/临时延期/延期时长；可勾选「仅显示有延期任务」。
7. 新增辅助函数 `fmtDateLocal`、`getMonday`、`setSummaryDateRange`。

## 汇总页按日期改为临时事项维度，总延期时长去重

修改文件：`D:/workspace/TaskManager/index.html`

完成内容：
1. 方向2（按日期）列表改为展示临时事项，不再按任务/项目累计。
   - 按临时事项 `date` 落在日期范围内过滤。
   - 搜索支持项目号、任务名、事项描述。
   - 列表展示：日期、事项描述、延期时长、影响范围、影响任务、关联项目。
2. 新增辅助函数：
   - `getTempItemDelayHours(item)`：统一从 `durationValue`/`durationUnit` 或旧 `delayHours` 计算小时数。
   - `getTempItemAffectedTasks(item, tasks)`：返回临时事项影响的任务列表（`all` 为未完成，`specific` 为指定任务）。
3. 顶部统计去重：
   - 按日期：临时事项数、影响任务数、影响项目数、总延期时长（不重复）。
   - 按项目：任务总数、有延期任务数、延期记录数、计划调整次数、临时事项数、总延期时长（按临时事项去重，避免同一事项影响多任务被重复累加）。
4. 搜索框 placeholder 与「仅显示有延期任务」复选框随方向自动切换显示/隐藏。

注意：项目方向下每行任务仍按各自 `delayRecords` 展示明细，但顶部「总延期时长」使用关联的唯一临时事项汇总，避免重复。

## 任务列表筛选状态持久化

修改文件：`D:/workspace/TaskManager/index.html`（同文件）

完成内容：
1. 新增 localStorage 键：
   - `tm-filter-status`：状态筛选下拉已选状态。
   - `tm-filter-project`：项目筛选下拉已选项目。
   - `tm-hide-completed`：「隐藏已完成项目」复选框状态。
2. 新增辅助函数 `saveFilterState(key, value)` / `loadFilterState(key, defaultValue)` 用于读写。
3. 下拉选择/全选/清除时自动调用 `saveFilterState` 保存当前值。
4. `renderTasks` 初始化下拉时读取保存状态，并过滤掉已不存在的项目或状态值。
5. 「隐藏已完成项目」复选框改为通过 `onHideCompletedChange()` 统一保存并触发重绘。

注意：筛选状态与任务数据分离存储，导出/导入数据时不受影响。
