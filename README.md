# 项目任务与临时事项管理工具

一款轻量级的静态网页工具，用于管理项目待办任务和临时事项，支持智能延期计算和数据导入导出。

## 功能特性

### 核心功能

- **任务管理**：按项目维度管理待办任务，支持状态跟踪、实时搜索
- **临时事项**：记录会议、紧急任务等临时事项
- **延期计算**：自动计算临时事项对任务的影响
- **数据导入导出**：支持 CSV/JSON 格式的批量导入和数据备份
- **操作反馈**：所有增删改操作均有 Toast 提示，无需依赖弹窗确认
- **键盘快捷键**：Escape 关闭弹窗

### 延期计算规则

公式：`offset = 1 + floor((hours - 2) / 8)`（当 hours > 2 时），前 2 小时计 1 个工作日，之后每 8 小时加 1 个工作日。

| 累计时长 | 延期工作日 | 计算方式 |
|---------|-----------|---------|
| ≤ 2小时 | 1天 | 固定 1 天 |
| 10小时 | 2天 | 1 + floor((10-2)/8) = 1 + 1 = 2 |
| 18小时 | 3天 | 1 + floor((18-2)/8) = 1 + 2 = 3 |
| 26小时 | 4天 | 1 + floor((26-2)/8) = 1 + 3 = 4 |

- 自动跳过周末和法定节假日
- 支持调休工作日识别
- 节假日数据自动更新（来源：[holiday-cn](https://github.com/NateScarlet/holiday-cn)）

## 快速开始

### 直接使用

1. 下载 `index.html` 文件
2. 双击用浏览器打开
3. 开始使用

### 部署到 Web 服务器

将整个 `task-manager` 目录部署到任意 Web 服务器即可。

## 使用说明

### 任务管理

1. 点击「新建任务」添加任务
2. 填写项目号、任务名称、时间等信息
3. 在列表中查看任务状态和延期情况
4. 使用搜索框按项目号或任务名称快速筛选

### 创建临时事项

1. 切换到「临时事项」标签
2. 点击「新建临时事项」
3. 填写事项日期、时长（小时/天）
4. 选择影响范围：
   - **所有未完成任务**：自动影响所有未完成任务
   - **指定任务**：手动选择受影响的任务

> 延期时长由系统根据「时长」自动计算，无需额外填写。

### 查看延期影响

1. 切换到「审阅」标签
2. 点击任务的「延期记录」链接
3. 查看影响该任务的所有临时事项

### 数据导入

#### CSV 格式

```csv
项目号,任务名称,开始时间,计划完成时间,状态
PRJ-001,需求分析,2024-06-01,2024-06-15,未开始
PRJ-001,前端开发,2024-06-10,2024-06-25,进行中
```

#### JSON 格式

```json
[
  {
    "projectName": "PRJ-001",
    "taskName": "需求分析",
    "startDate": "2024-06-01",
    "plannedEndDate": "2024-06-15",
    "status": "未开始"
  }
]
```

#### 导入模式

导入 JSON 备份数据时，系统会提示选择导入模式：

- **覆盖导入**（点击「确定」）：清除当前所有数据，替换为导入的数据
- **合并导入**（点击「取消」）：保留当前数据，将导入的数据中 ID 不重复的部分追加进来

## 技术特点

- **零依赖**：纯原生 HTML/CSS/JavaScript，无任何第三方库
- **单文件部署**：整个应用在一个 HTML 文件中
- **离线可用**：数据存储在浏览器本地，无需网络
- **自动更新**：节假日数据从 CDN 动态加载

## 浏览器兼容

- Chrome 92+
- Firefox 95+
- Edge 92+
- Safari 15.4+

> 依赖 `crypto.randomUUID()` 生成唯一 ID，如需支持更旧浏览器可自行替换为降级方案。

## 文件说明

| 文件 | 说明 |
|------|------|
| `index.html` | 主应用文件 |
| `DESIGN.md` | 技术方案文档 |
| `README.md` | 项目说明文档 |
| `sample-data.json` | 完整示例数据 |
| `sample-tasks.csv` | CSV 批量导入示例 |
| `sample-tasks.json` | JSON 批量导入示例 |

## 数据备份

### 导出数据

1. 切换到「数据管理」标签
2. 点击「导出数据 (JSON)」
3. 保存下载的 JSON 文件

### 导入数据

1. 切换到「数据管理」标签
2. 点击「导入数据 (JSON)」
3. 选择之前导出的 JSON 文件

## 常见问题

### Q: 数据会丢失吗？

A: 数据存储在浏览器 localStorage 中，清除浏览器数据会导致丢失。建议定期导出备份。

### Q: 能多人使用吗？

A: 当前版本为单机版，数据存储在本地浏览器。如需多人协作，需要接入后端服务。

### Q: 节假日数据准确吗？

A: 节假日数据来源于 [NateScarlet/holiday-cn](https://github.com/NateScarlet/holiday-cn)，自动从国务院公告抓取。2024-2026年数据准确，2027年以后为预估数据。

### Q: 如何修改延期计算规则？

A: 修改 `index.html` 中的 `hoursToWorkingDaysOffset` 函数即可。

## 开源协议

MIT License
